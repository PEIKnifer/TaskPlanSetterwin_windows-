---
description: 全能 .NET 开发核心协作与工程原则（总则）
alwaysApply: true
---

# 全能 .NET 开发：核心规则（总则）

## 输出与协作契约
- 默认使用**中文**沟通；用户明确要求英文时才切换。
- 先用 1–3 句复述目标与约束；仅在会导致实现方向分叉的关键前提缺失时，提出**≤2 个**澄清问题。
- 代码改动遵循**最小影响面**：不做无关重构/优化/改名；如果必须做结构调整，先说明“为什么这是必要的最小代价”。
- 给出可复核交付：明确**改了哪些文件**、**为什么改**、以及**最小验证步骤**（能在本机复现/回归）。

## 工程通用硬约束（含插件/宿主类项目）
- 默认把“运行环境”当作不可信且变化的边界：OS/运行时版本差异、权限、部署方式、依赖冲突都需要被显式考虑。
- 禁止“掩盖问题式兜底”：吞异常、静默失败、无限重试、超时强拉、默认值硬顶等都视为**缺陷**；优先定位根因并建立观测（日志/诊断信息）。
- 当项目属于**插件/扩展/宿主加载**场景时：
  - 把宿主当作边界：版本差异、加载顺序、沙箱/权限、依赖冲突需要显式建模；
  - 避免全局副作用：不要静默修改全局 `AppDomain`/`AssemblyResolve`/环境变量/线程池设置；若确需，必须可控、可回滚、可观测。

## 变更与验证基线
- 每次改动至少满足其一：能构建（`dotnet build`）或能跑测试（`dotnet test`）或提供可执行的手工验证步骤。
- 对外暴露的行为变更，必须同步更新：错误语义、日志字段、配置项含义或版本策略（按任务需要最小化更新）。
