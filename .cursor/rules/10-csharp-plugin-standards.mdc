---
description: C#/.NET 编码规范（可维护性、可靠性、线程与资源）
globs: **/*.cs
alwaysApply: false
---

# C#/.NET 编码规范

## 可维护性与 API 设计
- 默认开启并遵守可空引用：工程层面优先 `Nullable=enable`；不要用 `!` 逃避建模，除非边界已验证且有清晰不变量。
- 公共 API 必须清晰：输入/输出、异常语义、线程上下文要求（是否可在后台线程调用）要可推断或有文档化契约。
- 禁止随手暴露可变集合；对外用只读视图或复制；跨线程共享对象必须明确同步策略。

## 异常与日志
- 异常用于“真正的异常流”；不要用异常做常规控制流。
- 禁止吞异常：若必须捕获，至少记录**上下文 + 原异常**，并决定“重抛/转换/返回可诊断错误”之一。
- 日志必须可用来定位线上问题：包含插件版本、宿主版本（若可得）、关键参数与关联 ID（按场景）。

## async/await 与取消
- 可取消的 I/O 任务应接受 `CancellationToken`；不要忽略 token。
- 避免死锁：库代码默认 `ConfigureAwait(false)`（除非明确需要回到特定上下文）。

## 资源与生命周期（长期运行进程尤其重要）
- 任何订阅（事件/回调/Timer/Observable）必须可取消并在卸载/释放路径中解除。
- 实现 `IDisposable`/`IAsyncDisposable` 时遵循标准模式；使用 `SafeHandle` 管理非托管句柄。
- 避免静态持有宿主对象导致泄漏；能弱引用就弱引用，能显式释放就显式释放。
