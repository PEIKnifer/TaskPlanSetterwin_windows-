---
description: Public API、兼容性与版本策略（插件对宿主的契约）
globs: **/*.cs
alwaysApply: false
---

# Public API、兼容性与版本策略

## 破坏性变更（Breaking Change）判定
- 任何 `public/protected` 的：签名变更、可见性变更、行为语义变更、默认值变化、异常语义变化、线程安全语义变化，都可能是破坏性变更。
- 插件对宿主通常是“被动加载”：宁可新增能力（向后兼容）也不要替换既有语义。

## 弃用（Deprecation）优先于删除
- 优先走“弃用→过渡→移除”：
  - 先新增替代 API；
  - 对旧 API 标记 `[Obsolete]`（说明替代项与移除计划）；
  - 在下一次**主版本**再移除（如使用语义化版本）。

## 版本号与发布节奏（通用建议）
- 默认采用语义化版本（SemVer）：`MAJOR.MINOR.PATCH`。
- 不确定是否破坏兼容时，默认按“更严格”处理：宁可提高 MAJOR 或提供兼容层，也不要让宿主在运行期崩溃。

## 依赖与二进制兼容提示
- 避免让公共 API 暴露第三方类型（否则会把依赖版本冲突传播到宿主）。
- 若必须依赖第三方库，优先隔离到内部层并提供自有抽象（减少宿主依赖地狱）。
