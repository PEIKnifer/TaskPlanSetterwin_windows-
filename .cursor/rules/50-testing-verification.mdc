---
description: 验证与测试基线（插件回归、宿主兼容与可观测性）
alwaysApply: true
---

# 验证与测试基线

## 最小验证要求
- 任意代码变更必须附带至少一项：
  - 能在本机 `dotnet build` 通过；或
  - 能在本机 `dotnet test` 通过（若有测试）；或
  - 明确的手工验证步骤（含输入、预期输出、失败表现）。

## 插件特有验证（建议）
- 明确“宿主矩阵”：至少覆盖一个最低支持版本 + 一个当前主流版本（若任务相关且可获得）。
- 验证加载/卸载路径：重复加载、重复卸载、异常中断、并发调用（按宿主能力）。
- 验证可观测性：发生错误时日志能定位（版本、宿主、关键参数、异常）。
